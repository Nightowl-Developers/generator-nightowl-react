import * as React from 'react';
import classNames from 'classnames';

import 'style.css';

const <%= name %> = (props) => {
    return <div>
        <p>Functional Component</p>
    </div>;
};

export default <%= name %>;
