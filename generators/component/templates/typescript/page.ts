import React, { useState, useEffect } from 'react';

interface <%= name %>Props {};

const <%= name %> = (props): React.FunctionalComponentElement<<%= name %>Props> => {
    return <div>
        <p>Functional Component</p>
    </div>;
};

export default <%= name %>;