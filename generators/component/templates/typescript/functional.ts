import React from 'react';

interface <%= name %>Props {};

const <%= name %> = (props: <%= name %>Props): React.FunctionalComponentElement<<%= name %>Props> => {
    return <div>
        <p>Functional Component</p>
    </div>;
};

export default <%= name %>;